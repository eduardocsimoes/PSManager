(function($) {
    $.extend({
       tablepaginatorPager: new function() {
           
       } 
    });
    
    // extend plugin scope
    $.fn.extend({
        tablepaginatorPager: $.tablesorterPager.construct
    });	    
})(jQuery);
